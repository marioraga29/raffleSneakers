/*
 * DO NOT EDIT THIS FILE, it is auto-generated. It will be updated automatically.
 * All changes done to this file will be lost upon re-running the 'silence createapi' command.
 * If you want to create new API methods, define them in a new file.
 *
 * Silence is built and maintained by the DEAL research group at the University of Seville.
 * You can find us at https://deal.us.es
 */

"use strict";

import { BASE_URL, requestOptions } from './common.js';

const rafflesAPI_auto = {

    /** Gets all entries from 'raffles' */
    getAll: async function() {
        let response = await axios.get(`${BASE_URL}/raffles`, requestOptions);
        return response.data;
    },

    /** Gets an entry from 'raffles' by its primary key */
    getById: async function(raffleId) {
        let response = await axios.get(`${BASE_URL}/raffles/${raffleId}`, requestOptions);
        return response.data[0];
    },

    /** Creates a new entry in 'raffles' */
    create: async function(formData) {
        let response = await axios.post(`${BASE_URL}/raffles`, formData, requestOptions);
        return response.data;
    },

    /** Updates an existing entry in 'raffles' by its primary key */
    update: async function(formData, raffleId) {
        let response = await axios.put(`${BASE_URL}/raffles/${raffleId}`, formData, requestOptions);
        return response.data;
    },

    /** Deletes an existing entry in 'raffles' by its primary key */
    delete: async function(raffleId) {
        let response = await axios.delete(`${BASE_URL}/raffles/${raffleId}`, requestOptions);
        return response.data;
    },
};

export { rafflesAPI_auto };